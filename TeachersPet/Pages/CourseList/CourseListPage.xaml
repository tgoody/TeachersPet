<Page x:Class="TeachersPet.Pages.CourseList.CourseListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Course List" Height="450" Width="800">

    <Page.Resources>

        <!-- TODO: Finish these styles -->
        <!-- Course button can probably be "based off" the semester header one -->

        <Style x:Key="CourseButton" TargetType="{x:Type ListViewItem}">
            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="CourseButtonClick" />
        </Style>

        <!-- TODO: I found this online, try to understand it Matty -->
        <ControlTemplate x:Key="BetterLvItems" TargetType="ListViewItem">
            <Border x:Name="border" Background="Transparent">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Disabled" />
                    </VisualStateGroup>
                    <VisualStateGroup x:Name="SelectionStates">
                        <VisualState x:Name="Unselected" />
                        <VisualState x:Name="Selected">
                            <Storyboard>
                                <ColorAnimationUsingKeyFrames Storyboard.TargetName="border"
                                                              Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                    <EasingColorKeyFrame KeyTime="0" Value="LightBlue"/>
                                </ColorAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                        <VisualState x:Name="SelectedUnfocused">
                            <Storyboard>
                                <ColorAnimationUsingKeyFrames Storyboard.TargetName="border"
                                                              Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                    <EasingColorKeyFrame KeyTime="0" Value="SkyBlue" />
                                </ColorAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
                <ContentPresenter/>
            </Border>
        </ControlTemplate>



        <Style x:Key="SemesterHeader" TargetType="{x:Type ListViewItem}">
            <Setter Property="Template" Value="{StaticResource BetterLvItems}"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
        </Style>
        <Style x:Key="ListStyle">
        </Style>
    </Page.Resources>



    <Grid>
        <TextBlock Text="Courses" FontFamily="Bahnschrift SemiBold" FontSize="36"></TextBlock>
        <ListView x:Name="ListView" Style="{StaticResource ListStyle}" Background="Transparent" BorderBrush="#00ABADB3" FontSize="24" Padding="0,0,0,1" Margin="0,47,0,0" FontFamily="Bahnschrift Light"/>
    </Grid>
</Page>
